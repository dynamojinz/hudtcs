{% extends "layout.html" %}
{% block title %} Hello, {{ request.user }} {% endblock %}

{% block topbar %}
<div class="topbar">
	我的项目
</div>
{% endblock %}

{% block content %}
<div class="list">

<div class="title">
	<span class="name">名称</span>
	<span class="name">主题数</span>
	<span class="last">说明</span>
</div>
<ul>
	{% for ut in user_of_teams %}
	<li>
		<span class="name"><a href="/teams/{{ ut.team.id }}/topics/list">{{ ut.team }}</a></span>
		<span class="name">{{ ut.team.topic_set.all|length }}</span>
		<span class="last">{{ ut.team.description }}</span>
	</li>
	{% empty %}
        <li> 您还没有参加任何项目, 请联系管理员加入项目。</li>
	{% endfor %}
</ul>
</div>

{% endblock %}
